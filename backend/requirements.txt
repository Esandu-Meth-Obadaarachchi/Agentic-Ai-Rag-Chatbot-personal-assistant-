# --- Web framework ---
fastapi>=0.115,<0.116
uvicorn[standard]>=0.34,<0.35
python-multipart>=0.0.9          # multipart form parsing (file upload / ingest)
pydantic>=2.9,<3
pydantic-settings>=2.5,<3

# --- Auth + data (Firebase) ---
firebase-admin>=6.5,<7           # verify ID tokens + Firestore access

# --- LLM / agent (Anthropic via LangChain + LangGraph) ---
anthropic>=0.40
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-anthropic>=0.3,<0.4
langgraph>=0.2,<0.3

# --- Retrieval (Voyage embeddings + rerank, Pinecone store) ---
voyageai>=0.3
langchain-voyageai>=0.1
pinecone>=5.0                    # official Pinecone client (v3 API)
langchain-pinecone>=0.2

# --- Document parsing for ingestion ---
pypdf>=5.0
python-docx>=1.1

# --- Observability (optional) ---
langsmith>=0.1

# --- HTTP ---
httpx>=0.27
